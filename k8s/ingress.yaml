apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: $(APP_NAME)-ingress
  namespace: nextcloud
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: $(INGRESS_HOST)
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: $(APP_NAME)
                port:
                  number: 80

